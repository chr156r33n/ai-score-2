# Interpretability module — ON HOLD

Spec and implementation for this module are **deferred**. Do not block eligibility, attention, credibility, or runner work on interpretability decisions.

**Planned role (from `instructions.md`):** Deterministic technical assessment — crawlability, indexability, structured data, metadata, etc. Primary input likely Screaming Frog exports (**format TBD**).

**Planned interface (sketch only):**

```
Input:  url list + TBD crawl artifacts
Output: interpretability_score (0–100), issues[]
Gap:    100 - interpretability_score
```

**When hold is lifted:** Resume Q&A on SF export contract and check catalog; update `spec/v1-spec.md` decision row and this file.
